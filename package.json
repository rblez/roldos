{
  "name": "roldos-mockup-generator",
  "version": "1.0.0",
  "description": "Generate product mockups for Roldós",
  "scripts": {
    "generate": "node generate-mockups.js"
  },
  "dependencies": {
    "puppeteer": "^21.0.0"
  }
}
